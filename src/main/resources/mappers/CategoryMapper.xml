<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mappers.CategoryMapper">

    <resultMap id="CategoryResultMap" type="models.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="notes" column="notes"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" parameterType="int" resultMap="CategoryResultMap">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <select id="findByName" parameterType="string" resultMap="CategoryResultMap">
        SELECT * FROM category WHERE UPPER(name) = UPPER(#{name})
    </select>

    <select id="findAll" resultMap="CategoryResultMap">
        SELECT * FROM category
    </select>

    <insert id="insert" parameterType="models.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, notes, created_at)
        VALUES (#{name}, #{notes}, CURRENT_TIMESTAMP)
    </insert>

    <update id="update" parameterType="models.Category">
        UPDATE category
        SET name = #{name},
            notes = #{notes},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM category WHERE id = #{id}
    </delete>

</mapper>
