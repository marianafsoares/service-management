<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mappers.ProviderInvoiceMapper">

    <resultMap id="ProviderInvoiceResultMap" type="models.ProviderInvoice">
        <id property="id" column="id"/>
        <result property="invoiceNumber" column="invoice_number"/>
        <result property="pointOfSale" column="point_of_sale"/>
        <result property="description" column="description"/>
        <result property="receiverCuit" column="receiver_cuit"/>
        <result property="invoiceType" column="invoice_type"/>
        <result property="invoiceDate" column="invoice_date"/>
        <result property="presentationDate" column="presentation_date"/>
        <result property="subtotal" column="subtotal"/>
        <result property="vat21" column="vat_21"/>
        <result property="vat105" column="vat_105"/>
        <result property="vat27" column="vat_27"/>
        <result property="vatPerception" column="vat_perception"/>
        <result property="grossIncomePerception" column="gross_income_perception"/>
        <result property="incomeTaxPerception" column="income_tax_perception"/>
        <result property="exemptAmount" column="exempt_amount"/>
        <result property="stampTax" column="stamp_tax"/>
        <result property="total" column="total"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <association property="provider" javaType="models.Provider">
            <id property="id" column="provider_id"/>
        </association>
        <association property="category" javaType="models.InvoiceCategory">
            <id property="id" column="category_id"/>
            <result property="description" column="category_description"/>
        </association>
    </resultMap>

    <select id="findById" parameterType="int" resultMap="ProviderInvoiceResultMap">
        SELECT pi.*, ic.description AS category_description
        FROM provider_invoice pi
        JOIN invoice_category ic ON ic.id = pi.category_id
        WHERE pi.id = #{id}
    </select>

    <select id="findAll" resultMap="ProviderInvoiceResultMap">
        SELECT pi.*, ic.description AS category_description
        FROM provider_invoice pi
        JOIN invoice_category ic ON ic.id = pi.category_id
    </select>

    <select id="findByProviderId" resultMap="ProviderInvoiceResultMap" parameterType="int">
        SELECT pi.*, ic.description AS category_description
        FROM provider_invoice pi
        JOIN invoice_category ic ON ic.id = pi.category_id
        WHERE pi.provider_id = #{providerId}
    </select>

    <insert id="insert" parameterType="models.ProviderInvoice" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO provider_invoice (
            invoice_number, point_of_sale, description,
            provider_id, category_id, receiver_cuit, invoice_type,
            invoice_date, presentation_date, subtotal,
            vat_21, vat_105, vat_27,
            vat_perception, gross_income_perception, income_tax_perception,
            exempt_amount, stamp_tax, total,
            created_at
        ) VALUES (
            #{invoiceNumber}, #{pointOfSale}, #{description},
            #{provider.id}, #{category.id}, #{receiverCuit}, #{invoiceType},
            #{invoiceDate}, #{presentationDate}, #{subtotal},
            #{vat21}, #{vat105}, #{vat27},
            #{vatPerception}, #{grossIncomePerception}, #{incomeTaxPerception},
            #{exemptAmount}, #{stampTax}, #{total},
            CURRENT_TIMESTAMP
        )
    </insert>

    <update id="update" parameterType="models.ProviderInvoice">
        UPDATE provider_invoice SET
            invoice_number = #{invoiceNumber},
            point_of_sale = #{pointOfSale},
            description = #{description},
            provider_id = #{provider.id},
            category_id = #{category.id},
            receiver_cuit = #{receiverCuit},
            invoice_type = #{invoiceType},
            invoice_date = #{invoiceDate},
            presentation_date = #{presentationDate},
            subtotal = #{subtotal},
            vat_21 = #{vat21},
            vat_105 = #{vat105},
            vat_27 = #{vat27},
            vat_perception = #{vatPerception},
            gross_income_perception = #{grossIncomePerception},
            income_tax_perception = #{incomeTaxPerception},
            exempt_amount = #{exemptAmount},
            stamp_tax = #{stampTax},
            total = #{total},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM provider_invoice WHERE id = #{id}
    </delete>

</mapper>
