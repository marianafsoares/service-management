# Icono de Bits&Bytes

Colocá el archivo `bits-and-bytes.ico` en esta carpeta antes de generar el ejecutable con Launch4j o el instalador con Inno Setup. El archivo no se versiona para permitir que cada implementación use su propio ícono.
